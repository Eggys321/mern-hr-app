# Copy this file to .env and fill in real values. Never commit the real .env.

# Base URL of the backend API (no trailing slash). Falls back to the
# production Render URL if unset - see src/utils/apiConfig.js.
VITE_API_URL=http://localhost:4040
